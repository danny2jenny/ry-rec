package com.rytec.rec.node;

/**
 * Created by danny on 16-11-17.
 * <p>
 * 1、可以绑定到任何的一个Channel上面
 * 2、每次发送指令的时间具有间隔
 * 3、使用轮询的方式查询设备状态
 * 4、实时的指令的优先级大于定时的轮询
 * 5、需要为每一个Channel建立一个device的队列
 */
public class NodeModbus {
}
